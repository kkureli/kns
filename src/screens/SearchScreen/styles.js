import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    padding: 10,
    height: 50,
    width: 200,
    borderWidth: 1,
    alignSelf: 'center',
    marginTop: 10,
    borderRadius: 10,
  },
});

export default styles;
